{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Manim Render Video (QL)",
            "type": "shell",
            "command": "cd ${fileDirname} && manim main.py $(cat dev_scene)",
            "problemMatcher": [
                "$python"
            ]
        },
        {
            "label": "Manim Render Video (s)",
            "type": "shell",
            "command": "cd ${fileDirname} && manim -s main.py $(cat dev_scene)",
            "problemMatcher": [
                "$python"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
